package assets

import root "github.com/cleanstartup/stack/web"

type Builder = root.Builder
type AssetRegistry = root.AssetRegistry
type AssetManifest = root.AssetManifest
type AssetEntry = root.AssetEntry

var NewBuilder = root.NewBuilder
